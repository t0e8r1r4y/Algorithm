#include <bits/stdc++.h>
using namespace std;

#define MAX_X 101
#define MAX_Y 101

int arr[MAX_X][MAX_Y];
int v[MAX_X][MAX_Y];
int dx[4]  = {1,-1,0,0};
int dy[4]  = {0,0,1,-1};

typedef pair<int, int> p;

int R, C, K;

int main(void){

    cin >> R >> C >> K;

    for(int i = 0; i < K; i++){
        int x, y;
        cin >> x >> y;
        arr[x][y] = 1;
    }

    for(int i = 1; i <= )

    return 0;
}